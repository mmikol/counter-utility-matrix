beautifulsoup4>=4.12
psycopg[binary]>=3.1
requests>=2.31

# Development
pytest>=7.0
ruff>=0.16          # the linter: pycodestyle, pyflakes, isort, bugbear ... (pyproject.toml)
pytest-cov>=7.0     # coverage; the bar is in pyproject.toml

# The default local build target (db/psql/cluster) is an embedded Postgres.
# Skipped where no wheel exists (linux/arm64 - i.e. inside the Docker image,
# which talks to a real postgres service via DATABASE_URL instead).
pgserver; sys_platform == "darwin" or platform_machine in "x86_64 AMD64"
