# Runtime, pinned: the image is built from this file, and two builds of one
# commit should ship the same code. The direct dependencies are pinned; the
# packages they pull in are not. Bump deliberately, run the suite, say so.
beautifulsoup4==4.15.0
psycopg[binary]==3.3.5
requests==2.34.2

# A host build only. The default local build target (db/psql/cluster) is an
# embedded Postgres; the image and CI never start one - they talk to a real
# postgres service over DATABASE_URL, or skip the database-bound tests - so both
# filter this line out rather than carry 33 MB they will not run. The marker
# keeps it off the platforms with no wheel (linux/arm64).
pgserver==0.1.4; sys_platform == "darwin" or platform_machine in "x86_64 AMD64"

# Development. The image installs pytest and pytest-cov from this block for
# `orchestrator.py test` and drops ruff and mypy (Dockerfile); CI installs all four.
pytest>=7.0
ruff==0.16.7        # the linter (pyproject.toml); pinned, since a new release finds new errors in old code
mypy==2.3.1         # the types (pyproject.toml); pinned, since a new release finds new errors in old code
pytest-cov>=7.0     # coverage; the bar is in pyproject.toml
