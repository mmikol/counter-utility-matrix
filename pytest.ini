[pytest]
testpaths = tests
# bare `pytest` (e.g. in the container) must find the repo root like
# `python -m pytest` does
pythonpath = .
markers =
    invariant: needs the built database (db/psql/cluster by default)
    validation: needs the network as well - compares us against third parties
