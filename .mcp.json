{
  "mcpServers": {
    "countrix": {
      "command": ".venv/bin/python",
      "args": ["-m", "db.mcp"]
    },
    "countrix-docker": {
      "type": "http",
      "url": "http://localhost:8020/mcp",
      "headers": {"Authorization": "Bearer ${COUNTRIX_MCP_TOKEN:-}"}
    }
  }
}
