{
  "mcpServers": {
    "counter-utility-matrix": {
      "command": ".venv/bin/python",
      "args": ["-m", "db.mcp"]
    },
    "counter-utility-matrix-docker": {
      "type": "http",
      "url": "http://localhost:8020/mcp",
      "headers": {"Authorization": "Bearer ${COUNTER_MATRIX_MCP_TOKEN:-}"}
    }
  }
}
