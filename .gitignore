# Python
__pycache__/
*.py[cod]
.venv/

# lint, tests, coverage
.ruff_cache/
.pytest_cache/
.coverage
.coverage.*

# the page caches and the embedded cluster: built, never committed
.cache-blizzard/
.cache-wiki/
.cache-counterpick/
/db/psql/cluster/

# the CSV mirror (scraped text and rates) and any stray export. The authored
# inputs are the exception: they cannot be re-fetched.
db/raw/
*.csv
!db/data/authored/*.csv

# what the sentry set aside; never committed
*.md.quarantined

# the bearer token, the uid/gid override
.env

# editors and the OS
.idea/
.vscode/
.DS_Store
._*
